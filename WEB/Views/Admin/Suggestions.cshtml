@model List<EnerjiTahmin.Models.Suggestion>
@{ ViewData["Title"] = "Gelen Öneriler"; Layout = "~/Views/Shared/_Layout.cshtml"; }

<div class="container mt-5">
    <h2 class="fw-bold mb-4">📬 Gelen İstek ve Öneriler</h2>
    
    <div class="card shadow border-0">
        <table class="table table-hover align-middle mb-0">
            <thead class="bg-light">
                <tr>
                    <th class="ps-4">Tarih</th>
                    <th>Gönderen</th>
                    <th>Konu</th>
                    <th>Mesaj</th>
                    <th>İşlem</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <td class="ps-4 text-muted small">@item.Tarih.ToString("dd.MM.yyyy HH:mm")</td>
                        <td class="fw-bold text-primary">
                            @(item.GonderenKisi != null ? item.GonderenKisi.AdSoyad : "Silinmiş Kullanıcı")
                        </td>
                        <td><span class="badge bg-secondary">@item.Konu</span></td>
                        <td>@item.Mesaj</td>
                        <td>
                            <a href="/Admin/DeleteSuggestion/@item.Id" class="btn btn-sm btn-outline-danger" onclick="return confirm('Silinsin mi?')">
                                <i class="bi bi-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>